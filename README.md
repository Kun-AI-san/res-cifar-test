##Steps for running the script:
1. pip install -r requirements.txt
2. For training, from root, command python ./training/training.py --batch-size 256 --epochs 50
3. For confusion matrix, from root, command python ./test/test.py --batch-size 1024
